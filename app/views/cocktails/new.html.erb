<h1>add a new cocktail!</h1>
<%= simple_form_for @cocktail do |d| %>
  <%= d.input :name %>
  <%= d.input :photo, as: :attachinary %>
  <%= d.button :submit %>
<% end %>
<%= link_to "go back", :root  %>
